service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Only allow servers to write/read from Firestore instance
      allow read, write: if false;
    }
  }
}
